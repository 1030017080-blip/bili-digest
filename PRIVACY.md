# 隐私说明

生效日期：2026-09-17

Bili Digest 是一个仅托管在 GitHub 上、自带密钥（Bring Your Own Key）的 Chrome 扩展。它没有 Bili Digest 账户、开发者运营的后端、分析服务、广告或遥测。

本项目使用 [MIT 许可证](LICENSE)。下面按数据经过你浏览器时实际会发生什么，准确描述数据流。

## 扩展处理的数据

取决于你使用的功能，Bili Digest 会处理：

- 当前 B站视频的规范化 URL 与 BV 号；
- 字幕文本与时间戳；
- 视频元数据，如标题、UP 主、简介与时长；
- 在字幕中选中的文字及附近字幕上下文；
- 某条带时间戳笔记附近的字幕上下文；
- 你请求翻译的内容；
- 你保存的笔记；
- AI 服务与 Obsidian 的配置，包括 API 密钥；
- 缓存的字幕、概览与翻译结果。

## 数据去向

### 字幕（B站官方字幕接口）

Bili Digest 直接从扩展向 B站官方字幕接口请求字幕，包括：

- 视频信息接口 `/x/web-interface/view` 与分P接口 `/x/player/pagelist`；
- 字幕轨道列表接口 `/x/player/wbi/v2` 与 `/x/player/v2`；
- 字幕正文 CDN（`*.hdslb.com`）。

请求会带上你在 Chrome 中登录的 B站会话 Cookie，因此需要登录才可见的 AI 字幕轨也能被解析。字幕始终来自 B站官方接口，不经过任何第三方代理。

### AI 服务（DeepSeek，默认端点）

Bili Digest 的默认 AI 服务是 DeepSeek。当你使用 AI 功能时，扩展会把以下内容直接发给 `https://api.deepseek.com`（或你在设置页配置的 OpenAI 兼容端点）：

- 生成「概览」时：完整字幕，连同相关的标题、UP 主、简介或时长；
- 生成「解释」时：选中的文字及附近字幕上下文；
- 做「翻译」时：按语义切分的小批字幕段（用于渐进式中译），或你要翻译的概览/笔记内容；
- 润色笔记时：笔记附近的字幕上下文与视频元数据。

请求直连 DeepSeek（或你配置的端点），使用你提供的密钥做 `Authorization: Bearer` 认证。本扩展的开发者不代理、不接收这些请求。DeepSeek 会按照其自身条款、隐私政策与保留策略处理这些数据。除非其条款与你的义务允许，请不要发送机密、个人或受监管的内容。

设置页提供了可编辑的「本地改造」提示词，用于把项目改写为使用其他 OpenAI 兼容服务或模型。该提示词会明确要求不要在任何源码、提交、日志、截图、提示词或聊天中包含 API 密钥。

### Obsidian（Local REST API，仅写本地）

当你在设置页配置了 Obsidian Local REST API（默认地址 `http://127.0.0.1:27123`）并主动导出时，Bili Digest 会把生成好的 Markdown 文档通过 `PUT /vault/...` 写入你自己的 **Obsidian 库**。导出只发生在你点击导出时，写入目标始终是本机 Obsidian 库，不会发往任何远程服务器。

## 本地存储与保留

Bili Digest 使用 Chrome 的本地扩展存储（`chrome.storage.local`），不使用任何云服务。

- AI 服务与 Obsidian 的设置及 API 密钥保存在当前 Chrome 个人资料的本地存储中。为降低风险，扩展还会把存储访问级别限制为「受信任上下文」，避免视频页面脚本读到密钥或缓存内容。
- 已保存的笔记存放在本机，直到你删除或清除扩展数据。扩展最多保留 100 条笔记。
- 最近的字幕、概览和分句翻译结果缓存在本地；缓存按月清理。

Chrome 扩展存储不是密码保险箱。任何能访问你浏览器个人资料或设备的人，都可能恢复本地保存的密钥或内容。请尽量使用受限密钥、设置消费上限，若设备或浏览器个人资料被入侵，请轮换或吊销密钥。

要删除数据：

- 在「笔记」标签中逐条删除已保存的笔记；
- 在「设置」页清除缓存摘要、删除全部笔记或重置扩展数据；
- 从 Chrome 移除扩展或其存储数据，以删除全部本地设置、密钥、笔记与缓存；
- 在 DeepSeek 控制台吊销密钥，以阻止其后续使用。

清除本地数据不会删除 DeepSeek 已处理或保留的信息。如需服务侧的删除，请使用 DeepSeek 各自的控制项。

## 权限说明

Bili Digest 按以下用途使用 Chrome 权限：

- `sidePanel`：在 B站视频页旁边显示 Bili Digest 界面；
- `storage`：在本地保存设置、密钥、笔记与缓存结果；
- `tabs`：识别并作用于当前 B站视频标签页；
- `scripting`：协调扩展的页面内控制（播放器下方工具栏、浮层面板、字幕跟随/跳转）；
- B站主机访问权限：读取当前视频的 URL 与元数据，并提供时间戳跳转控制；
- `api.bilibili.com` 与 `*.hdslb.com`：获取 B站官方字幕；
- `api.deepseek.com`：提供 AI 概览、解释、翻译与笔记润色；
- `127.0.0.1` / `localhost`：与本地 Obsidian Local REST API 通信。

Bili Digest 不使用这些权限监控通用浏览活动。

## 不出售、不做广告

Bili Digest 不出售个人信息、不构建广告画像、不与数据代理商共享数据，也不包含任何分析 SDK。

## 变更

与隐私相关的改动会记录在本文件及仓库历史中。请在安装新版本前审阅更新。

## 问题

本仓库不提供公开的支持或问答渠道。使用前请审阅本政策、源码以及各服务商的文档。如发现漏洞或意外密钥泄露，请按 [SECURITY.md](SECURITY.md) 中的私有流程处理。