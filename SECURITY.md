# 安全策略

## 受支持版本

Bili Digest 是一个仅托管在 GitHub 上的小型项目。安全修复直接应用于 `main` 分支上的最新代码，若发布了版本，也适用于最新的 GitHub release。旧版本快照不再提供支持。

## 私下报告漏洞

请不要通过公开的 Issue 或 Pull Request 发布漏洞细节、泄露的凭据、隐私视频信息或字幕数据。本仓库不接受公开的安全报告。

当本仓库的 **Security** 标签页开放 GitHub 的私有漏洞报告流程时，请通过该流程提交。若看不到私有报告入口，请通过仓库所有者的 GitHub 主页联系他们，请求一个私有报告渠道，但不要在公开消息中包含漏洞细节。私有报告中请仅包含以下内容：

- 受影响的版本或提交；
- 复现问题所需的最少步骤；
- 预期行为与实际行为；
- 安全与隐私影响；
- 若你有建议的话，附上修复建议。

请移除真实的 API 密钥、访问令牌、私有 URL、字幕、笔记和个人信息，改用脱敏值与公开测试内容。

本项目不承诺响应时间，也没有漏洞赏金计划。在公开披露前，请留出合理时间供我们调查和修复。

## 高优先级问题

示例包括：

- API 密钥或私密内容出现在源码、日志、截图或发布包中；
- 向文档列出的 B站、DeepSeek、本机 Obsidian 之外的网络来源发起请求；
- 通过字幕、元数据、服务错误或模型输出进行脚本或 HTML 注入；
- 越权访问文档列出的 B站范围之外的浏览数据；
- 意外传输笔记、字幕或凭据；
- 依赖或发布流程被攻破；
- 绕过本地数据删除或 AI / Obsidian 配置的管控。

## 用户安全指南

- 只从你信任的 GitHub 源码或 release 安装。
- 在加载更新前，先审查改动和打包后的文件清单。
- 尽可能使用专门的、受限的 API 密钥，并在服务端设置消费上限。
- 不要复用来自生产系统的密钥。
- 一旦设备、浏览器个人资料、安装包、日志或截图可能泄露了密钥，请立即吊销。
- 请记住，Chrome 的扩展本地存储不是加密的密码保险箱。

发布脚本使用显式的文件白名单，并对公开文件做常见凭据模式的扫描，但自动化检查无法发现每一个秘密。